---
description: Start a wf brainstorm workflow
argument-hint: '<topic>'
---

Load the skill file at `${CLAUDE_PLUGIN_ROOT}/skills/wf/brainstorm/SKILL.md` and follow its instructions to start a brainstorm workflow with the user's task: `$ARGUMENTS`
