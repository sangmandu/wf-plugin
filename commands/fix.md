---
description: Start a wf fix workflow
argument-hint: '<bug or task>'
---

Load the skill file at `${CLAUDE_PLUGIN_ROOT}/skills/wf/fix/SKILL.md` and follow its instructions to start a fix workflow with the user's task: `$ARGUMENTS`
