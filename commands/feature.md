---
description: Start a wf feature workflow
argument-hint: '<task>'
---

Load the skill file at `${CLAUDE_PLUGIN_ROOT}/skills/wf/SKILL.md` and follow its instructions to start a workflow with the user's task: `$ARGUMENTS`
