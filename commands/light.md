---
description: Start a lightweight wf workflow
argument-hint: '<task>'
---

Load the skill file at `${CLAUDE_PLUGIN_ROOT}/skills/wf/SKILL.md` and follow its instructions to start a light workflow with the user's task: `$ARGUMENTS`
